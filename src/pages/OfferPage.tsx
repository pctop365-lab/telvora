import LegalDocumentPage, { sellerPlaceholder } from '@/components/LegalDocumentPage';

const headings = [
  'Сведения о продавце', 'Общие положения', 'Товар и информация о товаре',
  'Оформление заказа', 'Цена', 'Способы оплаты', 'Доставка',
  'Получение товара', 'Возврат', 'Гарантия', 'Ответственность',
  'Порядок направления претензий', 'Персональные данные', 'Реквизиты продавца',
];

export default function OfferPage() {
  return <LegalDocumentPage
    title="Публичная оферта"
    intro="Страница-заготовка будущего договора розничной купли-продажи TELVORA."
    notice={<><strong>Документ находится в подготовке и будет опубликован в окончательной редакции до начала коммерческих продаж TELVORA.</strong> Эта страница не является окончательной публичной офертой и не содержит предложения заключить договор на незаполненных условиях.</>}
    sections={headings.map((title, index) => ({
      id: `section-${index + 1}`,
      title: `${index + 1}. ${title}`,
      content: <p>{title.includes('продавц') || title.includes('Реквизиты') || title.includes('претензий') ? sellerPlaceholder : 'Раздел будет заполнен и юридически проверен до начала коммерческих продаж.'}</p>,
    }))}
  />;
}
