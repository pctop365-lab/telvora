# Minimal fragment for the EXISTING HTTPS server_name telvora.ru vhost.
# This is an insertion point, not a replacement server block.
#
# Before including routes.nginx.conf, inspect and preserve the existing:
# - exact PHP/API/FastCGI locations;
# - upload locations and PHP execution denies;
# - ACME challenge location;
# - admin/security deny rules;
# - existing static asset/cache rules.
#
# The generated include owns only the known frontend routes and the final
# unknown-route 404. Do not leave another catch-all `try_files ... /index.html`
# or `error_page 404 =200` rule active in this server.
include /ABSOLUTE/PATH/TO/telvora-seo/routes.nginx.conf;
