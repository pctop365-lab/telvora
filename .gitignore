logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.bolt/

# Secrets / auth material
cookies.txt
login.json
migrate_telvora_secrets.php
migrate_telvora_secrets.zip
telvora_secrets_FINAL_*.zip

# Security tooling / reports
.security-tools/
SECURITY_AUDIT.md
telvora_lint_*.txt
telvora_lint_*.zip

# Backups / candidates
*BACKUP*
*backup*
*BEFORE*
*before*
*CANDIDATE*
*SERVER-CURRENT*
*WORKING*
*BROKEN*
*broken*
*damaged*
*.BACKUP
*.BACKUP-*
*.backup
*.backup.*
*.BEFORE-*
*.before-*
*.CANDIDATE
*.CANDIDATE-*
*.SERVER-CURRENT.*
*.WORKING-*
*.BROKEN-*
*.broken.*
*.damaged.*
src_backup/
src_backup.zip
src-BACKUP-*/
dist-BACKUP-*/
templates/*.BACKUP.*
telvora_logo_temp/

# Deploy / server snapshots
SERVER_HARDENED_*/
TELVORA_HARDENED_SERVER_*.zip
telvora_*_frontend.zip
telvora_*_FINAL*.zip
telvora_auth_FINAL_*.zip
telvora_editstate_FINAL_*.zip
telvora_nodbtest_FINAL*.zip
telvora_offset_FINAL*.zip
telvora_safeerrors_FINAL*.zip
telvora_transient_FINAL*.zip
telvora_server_price_FINAL*.zip

# Test / debug utilities
check-products.php
db-check.php
debug-manager.php
install-products.php
mysql-test.php
php_test.php
test_pdf.php
telegram_ping.php
telegram_bot.php
dark.txt

# Accidental shell / editor debris (root-level only)
/(
/item
/minRating
/Number(minPrice)
/setActiveCat(cat.slug)
/setFiltersOpen(false)
/setFiltersOpen(true)
/setMaxPrice(e.target.value)
/setMinPrice(e.target.value)
/setSort(e.target.value
/{
/setActiveCat(cat.slug)}
/setFiltersOpen(false)}
/setFiltersOpen(true)}
/setMaxPrice(e.target.value)}
/setMinPrice(e.target.value)}

# Generated / vendor
pdf/dompdf/vendor/
